<UserControl x:Class="Canvas.Controls.Example.PropertyEditors.DropDownUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <TextBox Text="{Binding Class.Bob}" Grid.Row="0" />
        <TextBlock Text="{Binding Class.Tom}" Grid.Row="1" />
        <TextBlock Text="{Binding Class.Lou}" Grid.Row="2" />
    </Grid>
</UserControl>

